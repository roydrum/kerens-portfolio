# Hero fonts

Place **DIN Condensed Bold** here so the hero title (KEREN / BOSHI) uses it:

- `DINCondensed-Bold.woff2` (preferred)
- or `DINCondensed-Bold.woff`

If these files are missing, the site falls back to **Barlow Condensed Bold** (loaded from Google Fonts).
